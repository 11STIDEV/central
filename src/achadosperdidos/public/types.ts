export type LostFoundPublicView = "painel" | "itens" | "devolvidos";

export type LostFoundPublicStats = {
  availableCount: number;
  returnedCount: number;
  totalCount: number;
  thisWeekCount: number;
};
